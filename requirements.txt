# Chit - Receipt Printer Spirit
# pip install -r requirements.txt

# Core
numpy>=1.20            # Array operations (dependency of many packages)
Pillow>=9.0            # Image generation for printing
httpx>=0.24            # HTTP client for OpenRouter API
openai>=1.0            # OpenAI/OpenRouter API client

# Printer
python-escpos>=3.0     # Thermal printer ESC/POS commands
pyusb>=1.2             # USB backend for python-escpos

# Voice input
sounddevice>=0.4       # Audio recording
faster-whisper>=0.9    # Local Whisper transcription

# Hands-free mode
openwakeword>=0.5      # Wake word detection ("Hey Jarvis")
webrtcvad-wheels>=2.0.10  # Voice activity detection (silence detection)

# Raspberry Pi only (uncomment on Pi)
# RPi.GPIO>=0.7         # GPIO for button input